services:
  webserver:
    image: nginx:latest
    container_name: nginx_production
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./html:/usr/share/nginx/html
      - ./config:/etc/nginx/conf.d
    restart: always
